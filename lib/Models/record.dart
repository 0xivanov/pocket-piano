class Record{

  final String name;
  final List<dynamic> midiSounds;
  final List<dynamic> midiSeconds;
  final double duration;
  Record({ this.name, this.midiSounds, this.midiSeconds, this.duration });
}